[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  # Skip secrets scanning for build output and documentation
  SECRETS_SCAN_OMIT_PATHS = "build/static/js/*.js,build/static/js/*.map,server/*,SYSTEM_ARCHITECTURE.md,src/lib/database.ts,src/pages/*.tsx"
